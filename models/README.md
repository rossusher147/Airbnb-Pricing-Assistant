# Models

This folder stores trained model artifacts and related files.

Examples include serialized models, tokenizers, label encoders, checkpoints, and metadata needed for inference. Treat these as build outputs unless you have a reason to version specific artifacts.
