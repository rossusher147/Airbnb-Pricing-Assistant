# Tests

This folder contains automated tests for the project.

Add unit tests, integration tests, and regression tests here so the data pipeline, model behavior, and API endpoints can be validated as the codebase grows.
