# Frontend

This folder is for the user-facing application.

It should contain the web client that talks to the API in `src/APA/app.py` or related backend routes. If you add a framework such as React, Vite, or Next.js, its app structure and build configuration should live here.
