# Source Code

This folder contains the importable Python application code for the project.

The main package lives in `src/APA/`. Keeping code under `src/` helps avoid accidental imports from the repository root and makes packaging behavior more predictable.
