# Data Processing Code

This folder contains Python code for loading, validating, cleaning, and transforming datasets.

The goal is to keep the data pipeline reproducible in code so notebooks can call shared functions instead of duplicating cleaning logic.
