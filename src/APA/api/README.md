# API

This folder is for backend API modules.

Use it for route definitions, request and response schemas, endpoint handlers, and API-specific utilities that support the application entry point in `app.py`.
