# Feature Engineering

This folder is for feature extraction and feature engineering logic.

Use it for code that converts cleaned data into the input representation expected by the model, including encoders, transformations, and feature-building helpers.
