# Raw Data

This folder is for untouched source datasets.

Files here should represent the original input data before cleaning, labeling, feature engineering, or normalization. Keeping raw inputs unchanged makes it easier to reproduce the pipeline and audit how the final training data was produced.
