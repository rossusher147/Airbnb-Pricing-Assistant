# Data

This folder stores project datasets and derived data files.

- `raw/` holds the original source data exactly as collected.
- `interim/` holds partially cleaned or transformed datasets.
- `processed/` holds final, model-ready datasets used for training or evaluation.

Keep large data files out of version control unless there is a clear reason to track them.
