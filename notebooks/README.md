# Notebooks

This folder is for Jupyter notebooks used during experimentation.

Typical uses include:

- exploratory data analysis
- dataset cleaning experiments
- feature analysis
- model training and evaluation

As the project matures, reusable logic should move into `src/APA/` and be imported into notebooks rather than being defined only inside notebook cells.
